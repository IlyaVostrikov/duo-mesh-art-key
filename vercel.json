{
  "buildCommand": "cd webapp && bun run build",
  "outputDirectory": "webapp/dist",
  "installCommand": "bun install",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
